name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  test:
    name: Test tagged source
    runs-on: depot-ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test

  push:
    name: Bump manifest and push
    needs: test
    runs-on: depot-ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22
          cache: npm
      - name: Synchronize release metadata
        id: version
        uses: adversarylabs/actions/version@0a1079b58cb4aa289df240a12a22afcc5b51ce01
        with:
          tag: ${{ github.ref_name }}
          path: .
          branch: main
          token: ${{ secrets.RELEASE_GITHUB_TOKEN }}
          sync-npm: auto
      - name: Push adversary
        uses: adversarylabs/actions/push@db157870effc0c6c4f254b315514cb7d0fc74a2a
        with:
          cli-version: 2026.7.20-beta.1
          path: .
          token: ${{ secrets.ADVERSARY_SERVICE_ACCOUNT_TOKEN }}
          registry-namespace: adversarylabs
          repository-name: ${{ steps.version.outputs.name }}
          push-latest: true
